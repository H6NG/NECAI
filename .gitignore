venv/
venv
.env
necai/

# Build artifacts — regenerated by `make`, must never be committed.
# A stale committed binary can make `make` think it's up to date and skip
# the rebuild, leaving a wrong-architecture executable in the deploy.
*.o
necai_engine
top_moves
eval_stream

__pycache__/
